import 'package:flutter/material.dart';

class CustomFailedState extends StatelessWidget {
  const CustomFailedState({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Failed to load labs'));
  }
}